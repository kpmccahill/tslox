/**
 * Scanner defs.
*/

export class Scanner {
    source: string;

    constructor (source: string) {
        this.source = source
    }

    public scanTokens (): string[] {
        var tokens: string[] = [];
        return tokens;
    }
}
